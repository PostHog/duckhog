# name: test/sql/errors/connection_errors.test
# description: Connection errors when Flight SQL server is unavailable
# group: [errors]

require posthog

# Attach succeeds even if endpoint is unreachable; queries should fail.
statement ok
ATTACH 'hog:memory?token=demo&flight_server=grpc://127.0.0.1:1' AS remote;

statement error
SELECT * FROM remote.main.test_data;
----
Not connected to remote server
